language: cpp

matrix:
  include:
    - os: linux
      sudo: false
      compiler: gcc

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.9
    - zlib1g-dev
    - lcov
    - ruby
    - rubygems

before_script:
  source .travis/before_script.sh

script:
  source .travis/script.sh
